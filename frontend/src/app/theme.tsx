import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#03AED2',
      light: '#68D2E8',
      contrastText: '#fff',
    },
    secondary: {
      main: '#FDDE55',
      light: '#FEEFAD',
    }
  },
});